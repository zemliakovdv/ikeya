# Nginx конфигурация для IKEYA Frontend
# Разместить в /etc/nginx/sites-available/ikea_front.conf
# Создать симлинк: ln -s /etc/nginx/sites-available/ikea_front.conf /etc/nginx/sites-enabled/
#
# Для включения кэширования добавьте в /etc/nginx/nginx.conf в блок http:
#   proxy_cache_path /var/cache/nginx/ikea_front levels=1:2 keys_zone=ikea_front_cache:10m max_size=100m inactive=60m use_temp_path=off;

upstream ikea_front {
    server 127.0.0.1:3000;
    keepalive 64;
}

server {
    listen 80;
    server_name 45.135.234.22;

    # Логи
    access_log /var/log/nginx/ikea_front_access.log;
    error_log /var/log/nginx/ikea_front_error.log;

    # Максимальный размер загружаемых файлов
    client_max_body_size 20M;

    # Проксирование на Next.js приложение
    # Приложение работает на корневом пути, поэтому используем rewrite для удаления префикса
    location /ikea_front/ {
        rewrite ^/ikea_front/(.*) /$1 break;
        proxy_pass http://ikea_front;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # Редирект с /ikea_front на /ikea_front/
    location = /ikea_front {
        return 301 /ikea_front/;
    }

    # Статические файлы Next.js (_next/static)
    location /ikea_front/_next/ {
        rewrite ^/ikea_front/_next/(.*) /_next/$1 break;
        proxy_pass http://ikea_front;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_cache ikea_front_cache;
        proxy_cache_valid 200 60m;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_background_update on;
        proxy_cache_lock on;
        add_header Cache-Control "public, immutable";
        add_header X-Cache-Status $upstream_cache_status;
    }

    # Статические файлы из public (assets)
    location /ikea_front/assets/ {
        rewrite ^/ikea_front/assets/(.*) /assets/$1 break;
        proxy_pass http://ikea_front;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_cache ikea_front_cache;
        proxy_cache_valid 200 60m;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_background_update on;
        proxy_cache_lock on;
        add_header Cache-Control "public, max-age=3600";
        add_header X-Cache-Status $upstream_cache_status;
    }

    # Статические файлы Next.js без префикса (для абсолютных путей в HTML)
    location /_next/ {
        proxy_pass http://ikea_front;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_cache ikea_front_cache;
        proxy_cache_valid 200 60m;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_background_update on;
        proxy_cache_lock on;
        add_header Cache-Control "public, immutable";
        add_header X-Cache-Status $upstream_cache_status;
    }

    # Статические файлы из public без префикса (для абсолютных путей в HTML)
    location /assets/ {
        proxy_pass http://ikea_front;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_cache ikea_front_cache;
        proxy_cache_valid 200 60m;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_background_update on;
        proxy_cache_lock on;
        add_header Cache-Control "public, max-age=3600";
        add_header X-Cache-Status $upstream_cache_status;
    }
}

