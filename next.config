/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Если статика лежит в /public/assets, ничего больше не нужно
  // Доп. опции можно добавлять сюда по мере надобности
};

module.exports = nextConfig;
